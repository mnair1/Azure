version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/datafence_app/
hooks:
  AfterInstall:
    - location: scripts/start_flask
      timeout: 300
      runas: root
  ApplicationStart:
    - location: scripts/start_server
      timeout: 300
      runas: root
  # ApplicationStop:
  #   - location: scripts/stop_server
  #     timeout: 300
  #     runas: root